{
  "name": "meridian-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper that ships Meridian's web UI as native iOS + Android apps, pointed at the hosted feed.",
  "scripts": {
    "build": "python build_www.py",
    "sync": "npm run build && npx cap sync",
    "add:ios": "npm run build && npx cap add ios && npx cap sync",
    "add:android": "npm run build && npx cap add android && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
