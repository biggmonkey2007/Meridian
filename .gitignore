# =====================================================================
#  SECRETS — never commit. Telegram session/creds, API keys, anything private.
#  (If you add a new secret file, add its name here BEFORE the next commit.)
# =====================================================================
*.session
tg_session*
telegram_api.txt
telegram*.txt
*_api.txt
*.key
*secret*
gemini*.txt
.env
.env.*

# ---- local runtime data / per-user config (belongs in %LOCALAPPDATA%\Meridian, not the repo) ----
channels.txt
muted_sources.txt
feed_base.txt
cache/
update/
webview/
*.lnk

# ---- build artifacts ----
build/
dist/
__pycache__/
*.pyc
*.pyo
*.spec

# ---- editor / OS / tooling ----
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# gazetteer build artifacts (the .json is committed; these are not)
cities1000.zip
cities_gaz.backup.json

# hosted-feed server output
feed_out/
hosted_*.json

summary_key.txt
